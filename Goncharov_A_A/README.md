Чтобы запустиить файл выполните в терминале
```bash 
cd Task2-WeatherGame && python3 manage.py runserver
```
Чтобы смотреть погоду надо вставить API ключ openweather (его можно найти в одном из моих комитов ._. )
```bash
export OPENWEATHER_API_KEY="ключик"
```
