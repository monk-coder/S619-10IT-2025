# BPE Subword Tokenizer Implementation

## Installation
1. Ensure Python 3.8+ is installed.
2. Create a virtual environment (optional):
   ```bash
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   venv\Scripts\activate     # Windows