## Mini-LLM: Decoder-only Transformer for Next-Token Prediction

Implementation of a GPT-like transformer language model from scratch using only NumPy.

### Requirements

```bash
pip install -r requirements.txt
