# Маленькая языковая модель (decoder-only Transformer) на NumPy

Реализация GPT-подобной модели с нуля на NumPy. Обучается предсказывать следующий токен (causal LM).

## Требования
- Python 3.7+
- numpy
- matplotlib
- tqdm

Установка зависимостей:
```bash
pip install -r requirements.txt