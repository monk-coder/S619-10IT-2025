# Языковая модель Transformer с нуля на numpy

Реализация decoder-only Transformer (как GPT) для генерации текста, обученная с нуля на numpy.

## Требования

- Python 3.8+
- numpy
- matplotlib
- tqdm

Установка зависимостей:
```bash
pip install -r requirements.txt